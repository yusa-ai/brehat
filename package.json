{
  "name": "mpa",
  "version": "0.1.0",
  "devDependencies": {
    "grunt": "^1.4.1",
    "grunt-cli": "^1.4.3",
    "grunt-contrib-compress": "^2.0.0",
    "grunt-contrib-jshint": "~0.10.0",
    "grunt-contrib-nodeunit": "~0.4.1",
    "grunt-contrib-uglify": "~0.5.0",
    "grunt-critical": "^2.0.1",
    "grunt-cwebp": "^3.0.3",
    "grunt-purgecss": "^4.0.3"
  },
  "scripts": {
    "build": "grunt cwebp && cwebp.cmd && grunt purgecss && grunt critical && grunt compress",
    "compress": "grunt compress",
    "server": "npx http-server -b -c 31536000"
  }
}
